# Skill Loop Progress
# Skill: extreme-software-optimization (input: profiling-software-performance baseline run baseline-f04f754)
# Target: jeffrey_emanuel_personal_site (Next.js 16 App Router site) — page-load performance on desktop + throttled mobile
# Total Passes: 20
# Started: 2026-09-04T02:55:00Z

## Status: IN PROGRESS — Pass 7 of 20

## Learnings that re-shape later missions
- Pass 1: a forced style+layout during the hydration commit is attributed to whichever JS reads layout first; deferring one reader just moves the ~600 ms (4x) to the next reader (project-card mount effect). The lever is the COUNT of forced layouts/style recalcs during load (84 / 93 on mobile home), found by trace attribution (scratchpad perf/trace-layouts.mjs), and the SIZE of each layout (DOM nodes, backdrop-filter surfaces, complex selectors).
- Host load during measurement was 275–400 (other processes); treat ±10–15 % as noise, require profile-level evidence (self-time moved/removed, bytes removed) for a win.

## Evidence (baseline, git f04f754, local `next start`, headless Chrome, 20 runs/scenario)
- home-mobile4x: LCP p50 820ms, TBT~ p50 815ms, script 845ms, 84 layouts/168ms, 93 style recalcs/440ms, long tasks 1.1s (one 0.9–1.2s task at ~1.3–1.6s = hydration + forced layout)
- article-interactive-mobile4x: TBT~ 1250ms, script 1309ms; `getScrollMetrics` at mount forces layout (~700ms self at 4x)
- projects-desktop: DOM 4860 nodes, heap 22MB, TBT 187ms (102 cards mount eagerly)
- Every route ships ~300KB gz JS incl. lib/content.ts (41KB gz, 130KB raw) and framer-motion; globals.css 49KB gz everywhere (article-scoped CSS included)
- Hotspot table + hypothesis ledger: scratchpad perf/baseline-f04f754/HOTSPOTS.md
- Measurement recipes: scratchpad perf/perf-bench.mjs (RUNS, SCENARIOS env), perf-profile.mjs, route-bytes.sh, cpu-attrib.mjs

## Missions (re-ordered after pass 1: prefer levers with deterministic evidence — bytes, DOM nodes, hydrated client components, animated-at-load element counts — because host load makes ms-level deltas noisy)
1. [done] Forced layout at mount (hero/scroll-to-top) — null result, mechanism re-attributed
2. three.js off phones: site-header must not dynamic-import header-icon-3d on coarse pointers / reduced motion / no WebGL (render the static icon directly); evidence: chunk 4dbb8ea7 (155 KB raw / 49 KB gz, 90 % unused) absent from the mobile coverage list
3. Tree-shake lib/content.ts: `sideEffects: ["**/*.css"]` in package.json; evidence: markdown-article route JS gz drops (fa39fda8-equivalent content chunk gone or shrunk); fix any import-for-side-effect this exposes
4. Cross-route chunk leakage: hero chunk on /tldr, tldr-grid chunk on /, af131f7e on /tldr — trace the shared-chunk graph and split by route (dynamic boundaries / import hygiene); evidence: coverage "100 % unused" chunks gone
5. Article-scoped CSS out of globals.css into per-article CSS files imported by the article components; evidence: non-article routes' CSS gz drops from 49 KB
6. /projects eager grid: progressive mount / content-visibility for the 102 cards; evidence: DOM nodes 4860 → ≤2500, heap 22 MB ↓
7. Hydration cost: count client components/"use client" boundaries hydrated on /; convert static presentational sections to server components or remove motion wrappers from static-once elements; evidence: hydration FunctionCall (563 ms at 4x) ↓, JS gz ↓
8. three-scene variant split: dynamic-import only the active scene variant; evidence: desktop three bytes ↓ (706 KB raw chunk, 60–72 % unused)
9. header-icon-3d hourly animations: import only the active one; defer WebGL probe to idle; evidence: chunk bytes
10. Entrance-animation churn: 102 style recalcs / 93 layouts during a 5 s mobile load are browser-scheduled animation frames (framer whileInView/initial-animate on many cards, GSAP orbit ticker forcing style 6×); reduce elements animating at load (stagger after idle, CSS animations with `animation-composition`, `content-visibility`); evidence: trace recalc/layout counts ↓
11. Style cost per recalc: selector audit (attribute/`:has`/`*` rules, `html.light` legacy), backdrop-filter surface count at load; evidence: styleDurationMs per recalc ↓ (trace)
12. tailwind-merge: `cn` fast path/cache; evidence: `ec@…` self time ↓ in profile
13. Images: LCP/priority audit, `sizes` on every next/image; evidence: no image competes with LCP, request count ↓
14. Fonts: article pages' extra families (weights, preload); evidence: font bytes ↓ on article routes
15. Service worker precache list → idle/deferred; evidence: first-visit request count during load ↓
16. tldr grid render cost: derive once/memoize, defer offscreen sections; evidence: tldr long task ↓, DOM ↓
17. Article scroll readers (`getScrollMetrics` in 8 articles + article-progress): defer to idle, single read; evidence: forced-layout count in trace ↓ on article route
18. Interactive article (cmaes) script time: IO-gate visualizations, avoid eager KaTeX; evidence: article JS gz + TBT ↓
19. Re-profile + second-order hotspots (rerun perf-profile/trace, fix the new top item)
20. Perf budget regression gate: test asserting per-route JS gz bytes and homepage DOM/hydration budgets

## Completed Passes
### Pass 1 — Forced layout at mount (hero) — 2026-09-04T03:05Z
- Files changed: components/hero.tsx, components/scroll-to-top.tsx
- Changes: initial scrollY / getScrollMetrics reads deferred to an idle slice with cleanup; listeners unchanged
- Measured: home-mobile4x n=20 layout 168→163 ms, TBT p95 916→916, script 845→805 — within noise; hero self-time 619 ms → 15 ms but the same cost appeared under the next mount-time reader
- Commit: see git log "perf(loop 1/20)"
- Verdict: PRODUCTIVE (correct, free) but NO MEASURED WIN — mechanism re-attributed

### Pass 2 — three.js off phones — 2026-09-04T03:25Z
- Files changed: components/site-header.tsx
- Changes: StaticHeaderIcon exported from site-header; coarse-pointer/reduced-motion external store with server snapshot "static"; HeaderIcon3D only rendered (and its chunk only requested) for fine-pointer clients after hydration
- Measured (deterministic): home-mobile4x JS wire 556 → 325 KB gz; raw JS parsed 2042 → 1164 KB (three core 706 KB + fiber 155 KB + 17 KB absent from mobile coverage); desktop unchanged (3D canvas present)
- Correction to HOTSPOTS ledger: three.js core WAS shipping to phones via the header icon (the mobile ledger said only the 155 KB glue chunk)
- Commit: "perf(loop 2/20)"
- Verdict: PRODUCTIVE — WIN

### Pass 3 — Tree-shake lib/content.ts — 2026-09-04T03:45Z
- Files changed: none (both levers tried and reverted)
- Findings: Turbopack already removes exports unused by ANY client module (featuredSites/threads absent), but emits the module as one unit; site-header/site-footer/use-keyboard-shortcuts (root layout) import navItems/siteConfig, so every route references the 43 KB gz chunk. `sideEffects: ["**/*.css"]` → byte-identical build. `experimental.turbopackTreeShaking` crashes 16.1.6 (`tree_shake/graph.rs:743 index out of bounds`).
- Verdict: ZERO-CHANGE (evidence-backed); lever moves to pass 4 = source split of siteConfig/navItems into a small module re-exported by content.ts

### Pass 4 — Split site chrome config (lib/site-config.ts) — 2026-09-04T04:20Z
- Files changed: lib/site-config.ts (new, 46 lines), lib/content.ts (re-exports), components/site-header.tsx, components/site-footer.tsx, hooks/use-keyboard-shortcuts.ts
- Measured (deterministic): markdown article 293 → 252 KB gz JS (−41 KB, −129 KB raw), cmaes article 382 → 341; content chunk no longer referenced by article routes; /contact /consulting /about unchanged because page-level client modules (contact page, newsletter-signup, consulting-intake-form, github-heartbeat, x-stats-card) still import siteConfig from lib/content
- Commit: d03b374 (auto-committed by the external process)
- Verdict: PRODUCTIVE — WIN

### Pass 5 — Finish chunk leakage — 2026-09-04T04:40Z
- Files changed: app/contact/page.tsx, components/newsletter-signup.tsx, components/consulting-intake-form.tsx, components/github-heartbeat.tsx (siteConfig import → lib/site-config)
- Measured (deterministic): /contact 295 → 253 KB gz, /consulting 374 → 332 KB gz; neither references the content chunk any more. x-stats-card stays on lib/content (needs heroStats)
- Part B finding: the "100 % unused" hero/tldr/cards chunks on the other route are App Router `<Link>` viewport prefetches (RSC prefetch requests precede them by 15–90 ms); the server module graph is already split per route. Not a leak; disabling prefetch would be a behaviour change — not applied.
- Verdict: PRODUCTIVE — WIN

### Pass 6 — Article-scoped CSS out of globals.css — 2026-09-04T05:00Z
- Files: app/globals.css (anchor block 1143–3445 removed, 2-line note left between the anchors); new components/{hoeffding,raptorq,overprompting,bakery,cmaes,barra,sm}-article.css + components/interactive-article-shared.css (viz type-scale `:is(...)` block, `.custom-scrollbar`, stability guardrails); the 8 article components import their scope file (+ shared); scripts/validate-interactive-article-scopes.ts now maps selectors to the new files, fails if any article selector is left in globals.css (comments stripped), checks the raptorq/cmaes duplicated `:is(.raptorq-scope, .cmaes-scope) .rq-progress-bar` rules stay identical, and checks each component's imports
- Isomorphism: every rule byte-identical (sha256 of original == sha256 of globals[1..1142] + new-file ranges + tail); cascade order preserved (scope → shared for the six scopes, shared → sm for slack/wills, verified in the served `<link>` order); 16 screenshots before/after: only randomized canvas pixels differ (raptorq hero particle field, hoeffding scatter band), text/layout identical
- Measured (deterministic): CSS gz / /tldr /projects /about 50 → 44 KB; markdown article 55 → 48; interactive articles 55 → 51 (katex ones) and 51 → 47/48; globals chunk 413 → 381 KB raw / 49.5 → 43.5 KB gz
- LEARNING that re-shapes mission 11: the remaining globals chunk is 90.9 % `@layer utilities` (347 KB of 381 KB raw) — Tailwind v4 emits every utility used anywhere in the source scan on every route; custom rules are now only 4.3 %. The next CSS lever is utility volume (arbitrary-value class sprawl in article components/visualizations), not custom CSS
- e2e note: tests/e2e/wills-estate-article.spec.ts "pricing calculator" fails against `next start` regardless of this change — it hooks `console.info("[pricing_calc_changed]")`, which the component only emits when NODE_ENV !== "production" (string absent from the production bundle); the other 18 article tests + theme-toggle pass
- Verdict: PRODUCTIVE — WIN (smaller than the ledger's 25–35 KB estimate because the block was ~6 KB gz, not the bulk of the CSS)
