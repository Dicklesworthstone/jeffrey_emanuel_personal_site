# Skill Loop Progress
# Skill: extreme-software-optimization (input: profiling-software-performance baseline run baseline-f04f754)
# Target: jeffrey_emanuel_personal_site (Next.js 16 App Router site) — page-load performance on desktop + throttled mobile
# Total Passes: 20
# Started: 2026-09-04T02:55:00Z

## Status: IN PROGRESS — Pass 1 of 20

## Evidence (baseline, git f04f754, local `next start`, headless Chrome, 20 runs/scenario)
- home-mobile4x: LCP p50 820ms, TBT~ p50 815ms, script 845ms, 84 layouts/168ms, 93 style recalcs/440ms, long tasks 1.1s (one 0.9–1.2s task at ~1.3–1.6s = hydration + forced layout)
- article-interactive-mobile4x: TBT~ 1250ms, script 1309ms; `getScrollMetrics` at mount forces layout (~700ms self at 4x)
- projects-desktop: DOM 4860 nodes, heap 22MB, TBT 187ms (102 cards mount eagerly)
- Every route ships ~300KB gz JS incl. lib/content.ts (41KB gz, 130KB raw) and framer-motion; globals.css 49KB gz everywhere (article-scoped CSS included)
- Hotspot table + hypothesis ledger: scratchpad perf/baseline-f04f754/HOTSPOTS.md
- Measurement recipes: scratchpad perf/perf-bench.mjs (RUNS, SCENARIOS env), perf-profile.mjs, route-bytes.sh, cpu-attrib.mjs

## Missions
1. Forced layout at mount (hero): defer the initial `window.scrollY` read in components/hero.tsx to a frame after paint; same in components/tldr-hero.tsx
2. Forced layout at mount (articles + shell): `getScrollMetrics()` initial calls in article-progress, scroll-to-top, and the 8 *-article.tsx progress effects → first-frame deferral; make getScrollMetrics read the minimum set once
3. Tree-shake lib/content.ts: mark the package side-effect-free (`sideEffects: ["**/*.css"]`) and verify per-route JS bytes drop; fix any import-for-side-effect this exposes
4. Client content slimming: after 3, ensure site-header/footer/shortcuts/palette pull only navItems/siteConfig/slim indexes; measure fa39fda8-equivalent chunk gone from article routes
5. Article-scoped CSS out of globals.css: move each `.<article>-scope` block into a CSS file imported only by that article component; measure globals.css gz on non-article routes
6. /projects eager grid: virtualize/lazily mount cards below the fold (content-visibility or progressive reveal) — DOM nodes and TBT
7. Hydration double renders: audit mount-time setState (mounted flags, capability detection, matchMedia) that trigger a second render of large trees; consolidate via useSyncExternalStore/initializers — style-recalc count
8. three-scene variants split: dynamic-import only the active variant (three-scene.tsx is 142KB source with ~40 variants) — three chunk bytes on desktop
9. header-icon-3d: 24 hourly micro-animations → dynamic import of the active one; defer the WebGL probe to idle
10. tailwind-merge cost: measure `cn` call volume on article/tldr renders; raise twMerge cache size / hoist static cn() results out of render (isomorphic)
11. below-the-fold `content-visibility: auto` + contain-intrinsic-size for homepage/tldr sections — layout/style time
12. Images: LCP candidate audit (hero avatar `priority` vs headline), `sizes` on all next/image, lazy attrs; featured-site previews
13. Fonts: article pages load 3 extra families — subset/weights/display, preload only the two shell fonts
14. framer-motion footprint: LazyMotion/domAnimation for the shell or replace motion on static-once elements — bytes + hydration
15. Service worker precache: 11 navigations fetched on first install compete with the page; defer precache to idle / reduce list
16. tldr page long task: tldr-tool-grid render cost (14 cards, synergy diagram) — memoize/derive once, defer offscreen sections
17. Interactive article (cmaes) script time: lazy-mount visualizations below the fold (they already use dynamic; check `ssr:false` + IO gating), avoid eager KaTeX
18. Style recalc churn during load: backdrop-filter/blur surfaces and animated gradients present at load; reduce initial compositor/style work
19. Re-profile + second-order hotspots: rerun perf-profile.mjs, attribute the new top-5, fix the top one
20. Perf budget regression gate: Playwright/vitest test asserting per-route JS gz bytes and homepage long-task budget against baseline numbers

## Completed Passes
(none yet)
